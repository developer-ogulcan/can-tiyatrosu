---
interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---
<div>
  <p class="font-display text-5xl md:text-6xl text-ink">{value}</p>
  <p class="mt-2 text-sm uppercase tracking-wide text-stone">{label}</p>
</div>
